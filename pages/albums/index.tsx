import React from 'react';
import { MainLayout } from '~/layouts/MainLayout';

export const Albums = () => {
    return (
        <MainLayout>
            Альбомы
        </MainLayout>
    );
};

export default Albums
